<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Top Rankers</title>
  <style>

body {
  background-color: #1f1f1f;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  font-family: 'Poppins', sans-serif;
  color: #fff;
  margin: 0;
}

.ranker-container {
  background-color: #222;
  border-radius: 10px;
  width: 80%;
  padding: 20px;
  min-height: 90vh;
  box-shadow: 0 0 10px rgba(255, 165, 0, 0.3);
}

.ranker-header {
  display: flex;
  justify-content: center;
  align-items: center;
  background: linear-gradient(45deg, #f5a623, #ff6348);
  border-radius: 25px;
  padding: 10px 0;
  position: relative;
}
.ranker-header:hover {
    background: linear-gradient(45deg, #ff6348, #f5a623);
}


.ranker-header h2 {
  color: #000;
  font-size: 18px;
  margin: 0;
  font-weight: 600;
}

.close-btn {
  position: absolute;
  right: 15px;
  top: 5px;
  font-size: 20px;
  background: transparent;
  border: none;
  color: #000;
  cursor: pointer;
}

.ranker-table {
  width: 100%;
  border-collapse: collapse;
  margin-top: 20px;
  text-align: center;
}
/* FIX HEADER */
.ranker-table thead {
  display: table;
  width: 100%;
  table-layout: fixed;
}
.ranker-table tbody {
  display: block;
  max-height: 75vh; /* adjust height */
  overflow-y: auto;
  overflow-x: hidden; 
  width: 100%;
}
/* ROW FIX WIDTH */
.ranker-table tbody tr {
  display: table;
  width: 100%;
  table-layout: fixed;
}
.ranker-table th, .ranker-table td {
  border: 1px solid #333;
  padding: 10px;
}

.ranker-table th {
  background-color: #1a1a1a;
  color: #ffa500;
  font-weight: 600;
}

.ranker-table td {
  background-color: #2a2a2a;
  color: #fff;
}

.icon {
  margin-right: 5px;
}
@media (max-width: 499px) {

  /* icon upar, text niche — WITHOUT breaking table */
  .th-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
  }

  .th-inner .icon {
    font-size: 18px;
  }

  /* column width control */
  .ranker-table th:nth-child(1),
  .ranker-table td:nth-child(1) {
    width: 30%;
  }

  .ranker-table th:nth-child(2),
  .ranker-table td:nth-child(2) {
    width: 52%;   /* Username biggest */
    word-break: break-word;
  }

  .ranker-table th:nth-child(3),
  .ranker-table td:nth-child(3) {
    width: 30%;
  }

  /* font & padding adjust */
  .ranker-table th,
  .ranker-table td {
    font-size: 13px;
    padding: 8px 4px;
  }
}


  </style>
</head>
<body>
  <div class="ranker-container">
    <div class="ranker-header">
      <h2>Top Rankers 🏆</h2>
    </div>

    <table class="ranker-table">
      <thead>
      <tr>
        <th>
          <div class="th-inner">
            <span class="icon">🏅</span>
            <span>Rank</span>
          </div>
        </th>
        <th>
          <div class="th-inner">
            <span class="icon">👤</span>
            <span>Username</span>
          </div>
        </th>
        <th>
          <div class="th-inner">
            <span class="icon">🚀</span>
            <span>Score</span>
          </div>
        </th>
      </tr>

      </thead>
      <tbody>
        <% if (topScorers.length > 0) { %>
            <% topScorers.slice(0, 50).forEach((scorer, index) => { %>
                <tr>
                    <td>#<%= scorer.rank %></td>
                    <td><i class="fa-solid fa-user"></i> <%= scorer.username %></td>
                    <td><%= scorer.question_score %></td>
                </tr>
            <% }); %>
        <% } else { %>
            <tr>
                <td colspan="4">No top scorers yet</td>
            </tr>
        <% } %>
      </tbody>
    </table>
  </div>
</body>
</html>
